<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smm.mapper.UserInfoMapper">


    <insert id="saveUser" parameterType="com.smm.entity.UserInfo">
        insert into user_info(id, name, sex, age, email, password, phone, address) values(#{id}, #{name}, #{sex} ,#{age} ,#{email}, #{password} , #{phone} ,#{address})
    </insert>
</mapper>